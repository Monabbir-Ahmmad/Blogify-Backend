<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Password</title>
    <style>
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: Arial, sans-serif;
        color: #333333;
        line-height: 2;
      }

      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        border-radius: 10px;
        box-sizing: border-box;
        border: 1px solid #e0e0e0;
      }

      .logo {
        font-size: 28px;
        color: #8b5cf6;
        text-align: center;
        padding: 30px;
      }

      .heading {
        font-size: 20px;
        color: #fff;
        text-align: center;
        background: #8b5cf6;
        padding: 30px;
      }

      .content {
        padding: 26px 40px;
      }

      p {
        margin: 16px 0 !important;
        font-size: medium;
      }

      .button {
        font-size: medium;
        display: inline-block;
        padding: 12px 40px;
        background-color: #8b5cf6;
        color: #ffffff !important;
        text-decoration: none;
        border-radius: 4px;
        margin: 16px auto;
      }

      strong {
        color: #8b5cf6;
      }

      hr {
        margin: 0;
        border-color: #8b5cf6;
      }

      .footer {
        font-weight: normal;
        color: gray;
        font-style: italic;
        text-align: center;
        padding: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2 class="logo"><%= appName %></h2>
      <h4 class="heading">Password Reset Request</h4>
      <div class="content">
        <p>Hello <%= username %>,</p>
        <p>
          We have sent you this email in response to your request to reset your
          password for your <strong><%= appName %></strong> account.
        </p>
        <p>To reset your password, please click the link below:</p>
        <a href="<%= url %>" class="button">Reset Password</a>
        <p>This link will be valid for the next 24 hours.</p>
        <p>Thank you for using <strong><%= appName %></strong></p>
      </div>
      <hr />
      <p class="footer">
        If you did not make this request, you can safely ignore this email.
      </p>
    </div>
  </body>
</html>
